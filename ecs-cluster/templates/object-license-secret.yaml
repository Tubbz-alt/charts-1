---
{{- if .Values.objectsvsLicenseFile}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-object-svs-license
  annotations:
    "helm.sh/hook": "pre-install"
    "helm.sh/hook-delete-policy": "before-hook-creation"
  labels:
    release: {{ .Release.Name }}
  namespace: {{ .Release.Namespace}}
data:
  object-services-license.xml: {{required "license xml file is required to create the secret" .Values.objectsvsLicenseFile | b64enc}}
{{- end}}